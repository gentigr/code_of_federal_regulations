name: xml_code_generator
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.17.5 <3.0.0'

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
dependencies:
  xml: ^6.1.0
  diff_match_patch: ^0.4.1 # TODO(security): unverified uploader
  # original files are not organized as a package, disallowed to be fetched
  #    git:
  #      url: https://github.com/google/diff-match-patch.git
  #      path: dart
  #      ref: master